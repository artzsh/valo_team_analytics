import{useEffect,useMemo,useState}from'react';import{useData}from'../App';import{Container,Hero}from'../components/PageShell';import{Fade,FilterBar,Loading,NoData}from'../components/Common';import{ComboList,ComboRow}from'../components/Cards';import{aggregateCombos,best,worst}from'../lib/metrics';import{filterMatches,rowsFromMatches}from'../lib/grouping';import{mapAsset}from'../lib/assets';import type{ActId}from'../types';
export function MapsPage(){const{matches,catalog,loading}=useData();const[act,setAct]=useState<ActId>('all'),[player,setPlayer]=useState(''),[map,setMap]=useState('');const maps=useMemo(()=>{const c=new Map<string,number>();matches.filter(m=>act==='all'||m.act===act).forEach(m=>c.set(m.map,(c.get(m.map)??0)+1));return[...c].sort((a,b)=>b[1]-a[1]).map(x=>x[0])},[matches,act]);useEffect(()=>{if(!maps.includes(map))setMap(maps[0]??'')},[maps,map]);const players=useMemo(()=>[...new Set(matches.flatMap(m=>m.rows.map(r=>r.player)))].sort(),[matches]);const fm=useMemo(()=>filterMatches(matches,act,player,map),[matches,act,player,map]),rows=rowsFromMatches(fm),normal=aggregateCombos(fm,'players'),roles=aggregateCombos(fm,'roles'),agents=aggregateCombos(fm,'agents'),bg=mapAsset(catalog,map).image;if(loading)return <Loading/>;return <div className="relative"><div className="pointer-events-none absolute inset-x-0 top-0 h-[450px] bg-cover bg-center opacity-20" style={{backgroundImage:`linear-gradient(to bottom,transparent,#080b12),url(${bg})`}}/><Container><div className="relative"><Hero eyebrow="Map room" title="Статистика по картам" copy="Проверяй лучшие связки состава, ролей и агентов на каждой боевой площадке."/><FilterBar act={act} setAct={setAct} fields={[{label:'Карта',value:map,set:setMap,options:maps},{label:'Обязательно включать',value:player,set:setPlayer,options:players,none:'Нет'}]}/>{!fm.length?<NoData/>:<Fade id={`${act}-${map}-${player}`}><div className="space-y-3"><ComboRow title="Лучшая команда" combo={best(normal)} rows={rows}/><ComboRow title="Худшая команда" combo={worst(normal)} rows={rows}/><ComboRow title="Лучшая команда по ролям" combo={best(roles)} rows={rows}/><ComboRow title="Худшая команда по ролям" combo={worst(roles)} rows={rows}/><ComboRow title="Лучший состав агентов" combo={best(agents)} rows={rows}/><ComboRow title="Худший состав агентов" combo={worst(agents)} rows={rows}/></div><div className="mt-8 grid gap-5"><ComboList title="Обычные составы" combos={normal} rows={rows}/><ComboList title="Составы по ролям" combos={roles} rows={rows}/><ComboList title="Составы по агентам" combos={agents} rows={rows}/></div></Fade>}</div></Container></div>}
