import{useMemo,useState}from'react';import{useData}from'../App';import{Container,Hero}from'../components/PageShell';import{Fade,FilterBar,Loading,NoData}from'../components/Common';import{ComboList,ComboRow}from'../components/Cards';import{aggregateCombos,best,worst}from'../lib/metrics';import{filterMatches,rowsFromMatches}from'../lib/grouping';import type{ActId}from'../types';
export function TeamsPage(){const{matches,loading,error}=useData();const[act,setAct]=useState<ActId>('all'),[player,setPlayer]=useState('');const players=useMemo(()=>[...new Set(matches.flatMap(m=>m.rows.map(r=>r.player)))].sort(),[matches]);const fm=useMemo(()=>filterMatches(matches,act,player),[matches,act,player]),rows=rowsFromMatches(fm),normal=aggregateCombos(fm,'players'),roles=aggregateCombos(fm,'roles');if(loading)return <Loading/>;return <Container><Hero eyebrow="Team matrix" title="Статистика составов" copy="Сравнивай сыгранные пятёрки и ролевые сочетания. Победа матча учитывается ровно один раз."/><FilterBar act={act} setAct={setAct} fields={[{label:'Обязательно включать',value:player,set:setPlayer,options:players,none:'Нет'}]}/>{error||!fm.length?<NoData/>:<Fade id={`${act}-${player}`}><div className="space-y-3"><ComboRow title="Лучшая команда" combo={best(normal)} rows={rows}/><ComboRow title="Худшая команда" combo={worst(normal)} rows={rows}/><ComboRow title="Лучшая команда по ролям" combo={best(roles)} rows={rows}/><ComboRow title="Худшая команда по ролям" combo={worst(roles)} rows={rows}/></div><div className="mt-8 grid gap-5"><ComboList title="Обычные составы" combos={normal} rows={rows}/><ComboList title="Составы по ролям" combos={roles} rows={rows}/></div></Fade>}</Container>}
