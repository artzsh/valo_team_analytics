import{motion}from'framer-motion';import type{ReactNode}from'react';import type{ActId}from'../types';import{ACT_OPTIONS}from'../config/acts';import{fmt}from'../lib/normalize';
export const Fade=({children,id='x'}:{children:ReactNode;id?:string})=><motion.div key={id} initial={{opacity:0,y:10}} animate={{opacity:1,y:0}} transition={{duration:.28}}>{children}</motion.div>;
export function NoData(){return <div className="glass rounded-2xl p-16 text-center"><div className="text-5xl">◌</div><p className="mt-4 font-display text-3xl font-bold uppercase text-slate-300">Данных нет :(</p></div>}
export function Loading(){return <div className="flex min-h-[55vh] items-center justify-center"><div className="text-center"><div className="mx-auto h-10 w-10 animate-spin rounded-full border-2 border-white/20 border-t-valorant"/><p className="label mt-4">Загружаем боевые отчёты</p></div></div>}
export function FilterBar({act,setAct,fields=[]}:{act:ActId;setAct:(a:ActId)=>void;fields?:{label:string;value:string;set:(x:string)=>void;options:string[];none?:string}[]}){return <div className="glass sticky top-[73px] z-40 mb-8 flex flex-wrap gap-3 rounded-2xl p-3"><Select label="Акт" value={act} set={v=>setAct(v as ActId)} options={ACT_OPTIONS.map(x=>[x.id,x.label])}/>{fields.map(f=><Select key={f.label} label={f.label} value={f.value} set={f.set} options={[...(f.none?[['',f.none]]:[]),...f.options.map(x=>[x,x])]} />)}</div>}
function Select({label,value,set,options}:{label:string;value:string;set:(v:string)=>void;options:string[][]}){return <label className="min-w-[150px] flex-1"><span className="label ml-1">{label}</span><select value={value} onChange={e=>set(e.target.value)} className="mt-1 w-full rounded-xl border border-white/10 bg-white/5 px-3 py-2.5 text-sm outline-none focus:border-valorant/70">{options.map(([v,l])=><option key={v} value={v}>{l}</option>)}</select></label>}
export function StatCard({label,value,note,accent=false}:{label:string;value:string;note?:string;accent?:boolean}){return <motion.div layout className={`glass rounded-2xl p-4 ${accent?'border-valorant/40 bg-valorant/10':''}`}><p className="label">{label}</p><p className="metric mt-2">{value}</p>{note&&<p className="mt-1 text-xs text-slate-400">{note}</p>}</motion.div>}
export const pct=(n:number)=>`${fmt(n,1)}%`;
